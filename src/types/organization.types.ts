export interface OrganizationBasicDto {
    id: bigint;
    code: string;
    name: string;
}

export interface OrganizationSummaryDto {
    id: bigint;
    code: string;
    name: string;
    //TODO
}